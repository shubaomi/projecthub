# ProjectHub is designed to run locally on 127.0.0.1.
# The startup scripts use 13000 for the frontend and 13001 for the backend.

# Backend port used by server/index.ts and the Vite API proxy.
PORT=13001

# Optional: set to true if file watching or HMR causes issues in your environment.
DISABLE_HMR=false
